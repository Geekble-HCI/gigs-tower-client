```python
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

```python
python pop-client.py --mqtt-broker {ip 주소} --type {게임유형}

# ex) python pop-client.py --mqtt-broker 192.168.71.40 --type 5 --tcp
```
